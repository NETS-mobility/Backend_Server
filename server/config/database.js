// === DB 계정정보 설정 ===

module.exports = {
    connectionLimit: 5,
    host: 'localhost',              // 로컬계정
    user: 'root',
    password: '1234',               // 비밀번호
    database: 'nets',
    multipleStatements: true
}
